# AI News 前端

静态 H5 页面，通过调用后端 API 展示 AI 资讯卡片。

## 访问地址

GitHub Pages 部署后，访问地址形如：

```
https://<GitHub 用户名>.github.io/<仓库名>/
```

## 启用 GitHub Pages

1. 在仓库的 **Settings → Pages** 中，将 Source 设置为 **GitHub Actions**。
2. 保存设置后，GitHub Actions 部署成功即可访问上述 URL。
